# LAB_APPS_VIEWS

Django project named CarsRental with one app handling:

- / → Home page message
- /about/ → About Car Rentals page
- /password/generate/ → Random 10-character password generator

## Run Project

```bash
python manage.py runserver